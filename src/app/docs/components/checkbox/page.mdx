import Link from "next/link";
import { InstallTabs } from "@/components/InstallTabs";
import { CheckboxBasicExample, CheckboxDisabledExample } from "@/components/exemples/Checkbox/CheckboxExamples";
import { CheckboxPropertiesTable } from "@/components/exemples/Checkbox/CheckboxPropertiesTable";
import { CheckboxDownloadCode } from "@/components/exemples/Checkbox/CheckboxDownloadCode";

# Checkbox

Un contrôle d'entrée qui permet à l'utilisateur de sélectionner ou désélectionner une option.

## Aperçu

<CheckboxBasicExample />

## Installation

<InstallTabs
  npm="npm install tailwind-merge lucide-react"
  yarn="yarn add tailwind-merge lucide-react"
  pnpm="pnpm add tailwind-merge lucide-react"
  bun="bun add tailwind-merge lucide-react"
/>

## Exemples d'utilisation

### État désactivé

<CheckboxDisabledExample />

## Propriétés

<CheckboxPropertiesTable />

## Code source

<CheckboxDownloadCode />