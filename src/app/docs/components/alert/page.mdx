import Link from "next/link";
import { InstallTabs } from "@/components/InstallTabs";
import { AlertBasicExample, AlertVariantsExample } from "@/components/exemples/Alert/AlertExamples";
import { AlertPropertiesTable } from "@/components/exemples/Alert/AlertPropertiesTable";
import { AlertDownloadCode } from "@/components/exemples/Alert/AlertDownloadCode";

# Alert

Affiche un message d'alerte callout pour attirer l'attention de l'utilisateur.

## Aperçu

<AlertBasicExample />

## Installation

<InstallTabs
  npm="npm install tailwind-merge lucide-react"
  yarn="yarn add tailwind-merge lucide-react"
  pnpm="pnpm add tailwind-merge lucide-react"
  bun="bun add tailwind-merge lucide-react"
/>

## Exemples d'utilisation

### Variantes disponibles

<AlertVariantsExample />

## Propriétés

<AlertPropertiesTable />

## Code source

<AlertDownloadCode />