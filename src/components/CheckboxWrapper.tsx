"use client";

import { Checkbox } from '@/design-system';
import type { CheckboxProps } from '@/design-system';

export function CheckboxWrapper(props: CheckboxProps) {
  return <Checkbox {...props} />;
}
